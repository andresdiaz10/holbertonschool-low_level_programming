#include "holberton.h"
/**
 * _abs - Compute the absolute value of an integer
 * @n: Number to compute
 *
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = -1 * n;
		return (n);
	}
	else
	{
		return (n);
	}
}
